# AI 双面试官：从 0 到 1 Todo

> 目标：用户录入目标公司和岗位 JD 后，由“技术面试官”和“HR/项目面试官”进行针对性模拟面试；系统使用题库工具出题，并将用户薄弱点沉淀为可查看、可修正的个人能力记忆。

## 0. 定义 MVP 边界

- [x] 首版只支持 Java 后端岗位，复用现有 `mds/` 题库。
- [x] 支持手工录入公司信息、粘贴岗位 JD、创建一次模拟面试。
- [x] 支持两个独立面试模式：技术面、HR + 项目面。
- [x] 支持文本问答；语音、简历解析、真实公司数据抓取放到后续版本。
- [x] 明确评分维度：技术深度、项目真实性、表达、岗位匹配度、风险项。
- [x] 明确不做“替用户虚构项目经历”；反馈应帮助用户补齐能力和表达。

## 1. 产品流程与页面

- [x] 设计首页入口：`开始模拟面试`、`公司与岗位`、`能力画像`、`历史报告`。
- [x] 创建公司录入页：公司名、行业、规模、阶段、业务模式、技术文化、招聘偏好、备注。
- [x] 创建岗位录入页：岗位名称、经验年限、职责、任职要求、完整 JD 原文。
- [x] 创建 JD 分析确认页：展示提取出的必备技能、加分技能、业务场景、重点考察项，允许用户编辑。
- [x] 创建面试配置页：选择公司、岗位、面试官、面试时长、难度和目标（摸底/冲刺）。
- [x] 创建对话式面试页：当前题目、追问提示、回答输入、结束面试和进度状态。
- [x] 创建报告页：总评、分项评分、关键证据、薄弱点、学习建议和下一轮推荐。
- [x] 创建能力画像页：按技能展示掌握等级、证据、最近观测时间和改进趋势。

## 2. 数据模型与隐私

- [x] 定义 `CompanyProfile`：行业、规模、业务、文化、重点能力、面试偏好。
- [x] 定义 `JobProfile`：JD 原文、结构化技能、职责、经验要求、业务关键词、评分权重。
- [x] 定义 `InterviewSession` 与 `InterviewTurn`：配置、题目、回答、Agent 判断、追问链路、评分。
- [x] 定义 `UserSkillMemory`：技能、熟练度、薄弱点证据、置信度、建议、更新时间、来源会话。
- [x] 区分公司/岗位公共配置和仅属于用户的面试记录、能力记忆。
- [x] 提供查看、编辑、删除个人记忆的入口；默认不将用户回答用于其他用户。
- [x] 为 API 增加身份认证与按用户的数据隔离。

## 3. 题库与检索工具

- [x] 保持 `mds/` 为题目事实来源，执行 `cd web && npm run build:data` 生成结构化数据。
- [x] 为题目补充可选元数据：技能标签、难度、适用年限、场景标签、可追问方向。
- [x] 实现 `searchQuestions(query, filters)`：按技能、关键词、难度和分类检索。
- [x] 实现 `getQuestionDetail(id)`：返回题目、参考答案、关联题和追问建议。
- [x] 实现 `getFollowUps(questionId, answerAssessment)`：依据回答缺口返回深挖问题。
- [x] 为检索结果保留题目来源，Agent 不得编造“题库中存在”的内容。

## 4. Agent 设计

- [x] 编写共同系统规则：基于公司、岗位、当前会话和用户记忆作答；不泄露系统提示词；给出可验证的评分理由。
- [x] 技术面试官策略：先验证岗位核心技能，再按回答质量决定基础追问、原理追问、场景追问或换题。
- [x] HR/项目面试官策略：围绕职责、贡献、数据、冲突处理、职业动机和稳定性追问，识别表述模糊与项目风险。
- [x] 实现深挖决策：核心技能答对才继续加深；非核心技能设置追问上限；明显薄弱时记录并切换题目。
- [x] 将岗位技能映射为权重，例如支付/高并发岗位提高一致性、幂等、限流、资损处理的权重。
- [x] 每轮输出结构化结果：`nextAction`、`question`、`assessment`、`scoreDelta`、`memoryCandidates`。
- [x] 加入结束条件：达到时长/题数、覆盖核心技能、用户主动结束或连续低质量回答。

## 5. 后端与 AI 接入

- [x] 选择并初始化服务端（建议 TypeScript + Node API，与现有 `web/` 分离为 `server/`）。
- [ ] 配置数据库：开发环境 SQLite，生产环境 PostgreSQL；使用迁移管理表结构。
- [ ] 实现公司、岗位、会话、报告、能力记忆的 CRUD API。
- [ ] 实现 Agent 编排接口：加载上下文 → 调用题库工具 → 请求模型 → 校验结构化输出 → 持久化回合。
- [ ] 使用服务端环境变量保存模型密钥，绝不暴露至 Vite 前端。
- [ ] 为模型调用加入超时、重试、限流、错误提示与用量日志。
- [ ] 记录 Agent 决策、工具调用和模型版本，方便排查评分异常。

## 6. 前端集成

- [x] 新增路由和导航，不破坏现有题库浏览、搜索和随机练习功能。
- [ ] 提供 JD 粘贴、分析中、分析结果确认和失败重试状态。
- [ ] 对话页支持流式回答展示、停止生成、网络错误恢复和会话续接。
- [x] 报告中将“结论”与“回答证据”关联，避免空泛评价。
- [x] 能力画像支持用户标记“已改善”或“该判断不准确”，作为后续 Agent 上下文。

## 7. 质量验证

- [x] 为 JD 解析准备至少 10 个样例：初级 Java、资深 Java、支付、微服务、高并发和非技术岗位。
- [ ] 为每类岗位定义期望的重点技能、题目范围和最大追问深度。
- [ ] 编写 Agent 场景测试：答得好、答得浅、答错、跑题、夸大经历、拒答与敏感个人信息。
- [ ] 验证记忆不会跨用户、跨岗位错误污染；删除后不会继续注入 Agent 上下文。
- [ ] 执行 `cd web && npm run build`，并完成移动端和桌面端人工验收。

## 8. 上线与迭代

- [ ] 首先灰度给少量用户，观察完成率、平均轮数、报告查看率和薄弱点纠正率。
- [ ] 建立成本指标：单场 token、工具调用次数、平均响应时间和失败率。
- [ ] 根据反馈迭代评分规则、题目标签和追问深度，不直接以模型分数作为唯一结论。
- [ ] 第二阶段：简历解析、语音面试、题目收藏/错题练习、公司模板库和多轮成长计划。
